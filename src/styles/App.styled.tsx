import styled from 'styled-components'

export const AppWrapper = styled.div({
  maxWidth: '1440px',
  padding: '1rem 2rem',
})

export const MarketListings = styled.div({
  display: 'flex',
  flexFlow: 'row wrap',
  gap: '10px',
  justifyContent: 'center',
})
